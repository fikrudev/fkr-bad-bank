/*'use client';
 
import { createContext, useState } from 'react';
  
export default function ContextProvider() {
   const UserContext = createContext({});
   const [user, setUser] = useState(null);
  return (<UserContext.Provider value={{users:['fikru']}}/>);
}*/